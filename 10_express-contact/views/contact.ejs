<h3>Daftar Mahasiswa</h3>

<table class="table">
  <% if (contacts.length === 0) { %>
  <tr>
    <td colspan="4">
      <div class="alert alert-danger" role="alert">
        <p class="fw-bold">Contact masih kosong</p>
      </div>
    </td>
  </tr>
  <% } %>
  <thead>
    <tr>
      <th scope="col">No</th>
      <th scope="col">Nama</th>
      <th scope="col">Jurusan</th>
      <th scope="col">Detail</th>
      <th scope="col">Delete</th>
    </tr>
  </thead>
  <tbody>
    <% contacts.forEach((contact, i) => { %>
    <tr>
      <th scope="row"><%= i + 1 %></th>
      <td><%= contact.nama %></td>
      <td><%= contact.jurusan %></td>
      <td>
        <a
          href="/contacts/<%= contact.nama %>"
          class="btn btn-info badge rounded-pill"
          >Detail</a
        >
      </td>
      <td>
        <a href="/contacts/delete/<%= contact.nama %>" class="btn btn-danger badge rounded-pill" onclick="return confirm('Yakin ingin menghapus data ini?')">Delete</a>
      </td>
    </tr>
    <% }) %>
  </tbody>
</table>
<a href="/contacts/add" class="btn btn-primary badge rounded-pill" id="insert"
  >Insert</a
>
